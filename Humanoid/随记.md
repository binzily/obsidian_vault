==前两个月主线：2.0 RL Training 半身行走==

## 3.26-3.27
- 定主线
- 配环境
- 看源码

## 3.30
- 任务：定死上半身，直接开始训练下半身。
要确定的问题1：policy本身输出20维（12腿+8臂），那么observation还要不要包括上半身？不包括的话，那么action也不需要包括上半身。
如果目的是只想练腿（把 locomotion policy 定义成“下肢控制器”，把上半身视为外部扰动和形态变化来源，而不是 policy 需要建模和控制的对象），就是这套policy迁移到任何一个相同关节的机器人中都能行走，不管上半身怎么动、状态如何的话，

## 3.31
- 思考：**phase_ratio**加到obs里面能让actor/critic由于他是常量，所以相当于一个偏置项。源代码没有这样做：每个 env 随机不同的 air_ratio；不同 task 共用同一个 policy，但 air_ratio 不同；后续做 gait 参数 curriculum / randomization。
- 是否能用**DAPO**的上下界解耦的思路做？

## 4.1
先确认了半身步态训练在正常规模下的损失曲线和回放表现基本正常，并据此整理了 TensorBoard 里真正值得关注的核心指标，同时补充了实验记录；随后把上身扰动从原来较分散的继承配置，整理成更明确的一套训练设定，**加入了手臂目标位姿随机化、骨盆质量和质心扰动，以及稀疏的骨盆推力，同时保持策略只控制下肢、但让价值网络感知这些扰动**；最后~~我把单卡训练路径中的设备同步问题和观测构造里的跨卡张量错误修通，排除了 cuda:1 下的启动失败，~~并成功拉起了 4096 个环境、10000 次迭代的单卡训练，日志、checkpoint 和 TensorBoard 标量都已经正常写出。

## 4.2
- 读 lite half walk env 和 lite half walk env cfg 改动的部分。
- 看phase3训练结果：play中机器人整体姿态有些后仰（大概6-7度），分析**body_orientation_l2和flat_orientation_l2 reward**占比较低，遂调高3倍权重，在训练——两个奖励有所改善，但是暴露新问题，在右脚落地的时候会明显的上半身往后倾斜，分析原因**symmetry_cfg**没打开，所以打开在训一版。
	- 看**部署**的C++代码（正在看），改部署的C++代码（已完成）。
- 训练 policy /部署 policy **频率**二者不一致的话，如果部署50HZ训练100HZ，首先会导致部署比训练多了一倍的动作保持时间，也多了一倍的高层延迟；历史obs时间尺度也会变，训练时 10 帧历史覆盖 10 * 0.01 = 0.1s，训练时 10 帧历史覆盖 10 * 0.01 = 0.1s。

## 4.3
- 问AI项目最有价值的地方在哪里
- symmetry_cfg加了之后play 直接倒地抽搐，并不是滑倒抽搐——把默认的**mirror_loss_coeff**从100减小到0.05再次训练——变成两只脚落地前一瞬间都会有轻微后仰——开启**AMP**在训练。

## 4.7
1. 先把“**启动同步**”改对了，不再用 broadcast_object_list 去广播 GPU 上的模型参数，而是让各个 rank 用一致的模型 seed 直接初始化出相同权重，并用更安全的 tensor-level broadcast 作为基础工具；再把“**梯度同步**”改成了明确的 all_reduce 平均流程，不再依赖危险的对象广播或激进的扁平化/融合路径，并在多卡下加了同步与更保守的梯度裁剪配置；最后把“rank 和 device 的关系”理顺了，让每个进程在进入分布式通信前就绑定到正确的 CUDA 设备，同时把环境 seed 和模型 seed 分开处理，避免环境完全重复、但又保证初始网络参数一致。

2. **解决了双卡训练的问题定义**
**核心结论**

这次双卡 `illegal memory access` 的问题，最终没有落到 IsaacLab 的 task 逻辑本身。更准确地说：

- IsaacLab 这一层只是启动 distributed training。
- `rsl_rl` 这一层也只是做 `torch.distributed` / `init_process_group("nccl")` 之类的初始化。
- 真正决定通信 transport / algo 的，是更底层的 NCCL 默认行为。
- 在我们这台机器和这类 humanoid locomotion workload 上，默认 NCCL 路径会触发崩溃；显式改 `NCCL_SHM_DISABLE=1` 等环境变量后，问题可以绕开。

所以这不是“IsaacLab 某个 task 配错了”，也不是“rsl_rl 里缺了这三个参数的代码开关”这么简单。更准确的说法是：**IsaacLab / rsl_rl 当前没有给用户一个显式的 NCCL 排障入口，而默认 NCCL 选择在部分机器上不稳定。**

**做了什么分析**

先是排除了自己代码里的明显问题。

- 修了 `tk_lab` 里 symmetry 路由错误，避免 `lite_walk` 观测错误地走到不匹配的 symmetry 实现。
- 修了 `tk_lab` 里分布式初始化/同步链路里几处不合理实现，包括 GPU 参数同步方式、seed 处理、梯度同步细节和设备绑定问题。

这些修完以后，双卡错误确实往后推了：
- 不再死在 Python 层的 broadcast / symmetry 维度错配
- first-fault 变成更底层的 `omni.physx.fabric` / `DirectGpuHelper.cpp` / `ProcessGroupNCCL` 一类 CUDA illegal memory access

这一步很重要，因为它说明：`tk_lab` 里原先确实有局部 bug，但它们不是最后那个双卡崩溃的主因。

**我们做了什么实验**

然后用**官方链路做最小复现**，目的是切开“平台问题”和“自定义 env 问题”。

做过的关键对照是：

- 官方 `Isaac-Cartpole-v0` 双卡 distributed：能跑
- 官方 `Isaac-Velocity-Flat-G1-v0` 双卡 distributed：默认会炸
- 你自己的 `tk_lab lite_walk` 双卡 distributed：默认也会炸
- 在github查找相关**Issue**发现加上
  - `NCCL_SHM_DISABLE=1`
  - `NCCL_IB_DISABLE=1`
  - `NCCL_ALGO=Ring`
  之后，官方 G1 和 `tk_lab` 的双卡短跑都能过

更关键的是做了更细的**对照归因**：
- 在这台机器上，`NCCL_SHM_DISABLE=1` 单独就已经足够让官方 `Isaac-Velocity-Flat-G1-v0` 的双卡最小复现通过
- `NCCL_IB_DISABLE=1` 和 `NCCL_ALGO=Ring` 更像附加 fallback，不是第一主因

结合 **NCCL debug** 输出，我们看到：
- 默认失败时，NCCL 实际走的是 `SHM/direct/direct`
- workaround 成功时，通信路径切到了 `NET/Socket/...`

这进一步支持一个判断：**问题更像是默认 NCCL SHM 路径在这台 Linux 双卡 5090 机器上的兼容性/稳定性问题，而不是 IsaacLab 的任务逻辑问题。**

**最终判断**

可以把最终判断压缩成这几句：

- 这个问题不是由 IsaacLab humanoid task 本身触发的逻辑错误。
- 也不是因为 IsaacLab 或 `rsl_rl` 代码里显式把 `NCCL_SHM_DISABLE`、`NCCL_IB_DISABLE`、`NCCL_ALGO` 设错了；实际上它们根本没有显式设置这三个变量。
- IsaacLab / `rsl_rl` 做的事情主要是初始化 distributed training，之后 NCCL 会按默认规则自行选择 transport / algo。
- 在我们本地的 humanoid locomotion 双卡场景里，正是这个默认 NCCL 选择出现了问题。
- 因此，最合适的 upstream 产出不是“硬改 IsaacLab 默认行为”，而是先补一条官方 troubleshooting 文档，告诉用户在受影响机器上如何通过 NCCL 环境变量规避问题。

**最终产出**

这次已经有 3 类产出。

1. 本地技术结论
- 证明了问题不只出现在 `tk_lab` 自定义 env
- 官方 `Isaac-Velocity-Flat-G1-v0` 双卡也能复现
- 确认 workaround 有效，尤其是 `NCCL_SHM_DISABLE=1`

2. 本地代码侧收敛
- 修掉了 `tk_lab` 中和这个问题混在一起的 symmetry / distributed 实现问题
- 让最后剩下的问题边界更清楚

3. 上游文档 PR
- 在 multi_gpu.rst 增加了 `Troubleshooting NCCL Errors`
- 文档内容只提供 workaround，不改默认行为
- PR 的叙事重点是：
  - 这是 NCCL 层面的排障说明
  - 不是 IsaacLab task 逻辑修复
  - 也不是给所有机器强行改默认值
```md
In our local reproduction, the failure did not appear to originate from IsaacLab task logic itself. IsaacLab and `rsl_rl` only initialize distributed training, while the NCCL transport/algo selection is handled below that layer. On our Linux dual-GPU setup, the default NCCL path was unstable for humanoid locomotion workloads, and the failure could be avoided by overriding NCCL behavior with environment variables such as `NCCL_SHM_DISABLE=1`.
```

3.开启AMP训练会导致Curriculum/terrain_levels降低，因为AMP的数据是平地专家数据。

## 4.8
1.AMP的训练效果不错。
2.继续看部署的代码——深入串联并联部分。

## 4.9
1.继续看部署的代码——跑 sim2sim，手柄还没到货。

## 4.10
1.看 issac lab 代码，看他的代码和 tk lab，找相似性，努力把 env-algo 范式打通。
2.跑 sim2sim 出现问题。

## 4.11
1.挖跑sim2sim 出现问题，找出原因：**时基**不一致。
准备改代码，统一这些写法。

## 4.12
1.“本地代理”决定 Claude Code 的流量会不会先进入 CC Switch；“全局出站代理”决定进入 CC Switch 之后，它再怎么出去。

## 4.13
1.时基不一致的问题改了更规范的代码解决。

## 4.14
1.webots sim2sim 比较成功。
目前的上半身扰动只涉及在训练中等扰动随机化，sim2sim是没有做域随机化以及taget pose随机化。

## 4.16 
1.搞懂了脚踝串并联反解，PD控制的本质。
2.xy命令为0,只有yaw命令的时候，也就是原地转，真机上机器人会有xy速度（webots仿真也是一样），转着转着就不在原地了。
对策：先做 yaw-only 的命令分布，actor obs 、 reward 等都不动，训练20k iteration。

## 4.17
1.yaw-only 感觉效果不理想，前倾严重，且x only和y only命令遵守也没有变好——还是没改 reward ，改成 axis only ，40k iteraion 再次训练，18号看效果。

## 4.18
1.关键发现：terrain_levels 随着 axis 的加入会越来越低——这是必然，因为terrain_levels 更新只看两件事：: XY位移 distance > 4m ；distance < ||cmd_xy|| * episode_len * 0.5。

## 4.19 
 1.axis only ，40k iteraion 效果：yaw only效果不错；x/y only 有点 roll（和命令同向）； 向前走上半身明显向前 pitch 。
 原因：
1. `axis-only` 把“纯单轴命令”从偶发角落变成了固定占比、持续 8-12s 的子任务（x-only/y-only 强制另外两轴为 0）。

2. `y-only` 下，抑制 roll 的两个正则是按 `|cmd_y|<0.1` 才生效，所以在 y-only 场景会被关掉。
3. 另外 axis-only 里把这批 env 的 heading 分支也关了（`is_heading_env=False`），少了一层约束。
4. 虽然 x/y-only 只占总 env 约 9%，但策略参数是共享的，这部分梯度会影响全局，所以会在前进时也看到 pitch 偏置。
5. AMP 的 motion file 可能也有问题 。
## 4.20
1.考虑：
两阶段训练（通用基线 + 轴向专项微调）
先训通用行走，再用更高比例的 axis-only 做短程 finetune（2k iter）.
结果：两阶段相比之前从头训练有改善，yaw only 的时候不倾斜了，但是 y only 的时候还是会往命令方向倾斜；所以得改 奖励函数（原步态奖励会鼓励正常步态走；抑制 roll 的两个正则是按 `|cmd_y|<0.1` 才生效）。这次实验证明在通用的 checkpoint 上 resume 少 iter 是有效的。

出现了一只脚为轴心，圆规转的问题。

## 4.21
得到任务：
1. ==2.0lite 的半身走不做了，开始做2.0 pro 的半身站走。==
2. ==2.0pro 的半身站走做完后，参考 move_operate 的奖励函数、命令设置来做 2.0pro 在半身站走的基础上做 roll、pitch、height 的 command 。（2.0 pro 的全身 roll、pitch、height 的command 的执行在目前AMP的基础上，上半身会做的很丑）==
3. 还有一个痛点就是切换状态的时候很僵硬。

先训练 pro half walk 一版本；同时训练了 move operate 看看roll、pitch、height command的效果。

## 4.22
1.处理 git 、 git worktree。

## 4.23
1.Webots做sim2sim，在Zero2MLP的时候，还没给手柄命令，机器人**全身抖动**。
分析问题：在 RobotInterfaceImpl::SetCommand 里面，之前的代码大概是这样：
```c++
部分关节:
  setTorque(PD torque)

另一些关节:
  q_d = 500000  // 跳过 setPosition
  tau_d = 500000 // 跳过 setTorque

同时又调用:
  setMotorPos(...)
  setMotorTau(...)

```
这里有几个问题：
1. 500000 不是控制策略，只是一个“别设置这个关节”的哨兵值。结果就是某些关节在 MLP 后可能保持 Webots 上一次 position target，某些关节切到 torque target，某些关节实际等于半开环。
2. Webots 的 setPosition() 是 Webots 内部位置伺服，不是我们训练里的 Isaac actuator PD。它内部还有自己的 motor solver、速度限制、力矩限制。你再同时对别的关节 setTorque()，整个机器人就不是一个统一的控制系统。
3. half-walk 训练时的假设是：12 个腿关节由 policy 给目标位，waist/arms 追默认位，所有关节都通过同一套 PD/隐式 actuator 行为进入物理世界。之前部署侧实际变成：腿、腰、手臂、手腕的控制模式不一致。
4. ZERO2MLP 的瞬间最容易暴露这个问题。ZERO 阶段是 position mode，机器人被拉到默认姿态；一进 MLP，部分关节突然切 torque，部分关节还残留 position servo 或不再更新。这个切换本身就是一个冲击源，所以摇杆不动也会原地抖。

现在改成统一显式 PD torque 后，语义变清楚了：
```c++
21 个训练合同关节：legs + waist + shoulder_pitch/roll/yaw + elbow_pitch
走 torque PD

6 个训练里不存在的额外关节：elbow_yaw / wrist_pitch / wrist_roll，左右各 3 个
只 position hold 默认位

训练过、policy 观测/输出覆盖的关节：可以 torque PD 跟 policy target
没训练过、policy 不知道的额外关节：position hold 默认位，Webots 内部自己处理位置保持，尽量从动力学里“安静掉”

所以手腕这种 policy 没训练过、也不参与主运动的额外关节，用 torque PD 可能会把速度估计噪声、微小位置误差、离散导数误差放大成抖动 torque。尤其 wrist/elbow_yaw 这种轻、小、惯量低的末端关节，kd 或速度噪声稍微不合适就会抖。

```
所以机器人不再被两套控制器撕扯，ZERO 到 MLP 的控制模式切换也一致了，抖动自然消失。

2.pro和lite在policy训练的时候的**sim.dt**不一致，前者是5ms，后者是2.5ms。

明天上机试试。
## 4.24
1.pro-half-walk 的右脚比左脚迈的步子大很多。可能是mirror_loss_coeff=0.05 导致 ，因为pro和half 的base reward 本身就有很多不一致。——把 mirror_loss_coeff 提到 5 ，解决问题。
2.为什么 pro-half-walk 的 Curriculum/terrain_levels 能到5 ：训练时基/episode 物理时长不同，速度 command 是 m/s：
lite: sim.dt=0.0025, decimation=4, step_dt=0.01, max_episode_length_s=10
pro : sim.dt=0.0050, decimation=4, step_dt=0.02, max_episode_length_s=20
3.randomize_apply_external_force_torque相比pro walk 来说，half pro walk 是没有启用的，后面要策略更鲁棒的话需要开启，同时**加大相关 event 事件扰动**。

## 4.25
1.改 domain randomazation ，对齐源配置，重新训练。 

## 4.27
1.pro half walk 训练完毕：原地转比 lite 强很多，我也没改配置，分析原因
- symmetry loss coeff pro 比 lite 大很多，转向依赖一定的 symmetry 
- pro 上半身有 waist yaw 这个关节，转向上身扭动、躯干稳定会影响下肢 yaw 的效果。
一个反证：TensorBoard 的全局 Episode_Reward/track_ang_vel_z_exp 并不显示 pro 明显更高，lite last500 约 1.60，pro_latest 约 1.27，pro_sym 约 1.47。所以看到的“原地转更好”更像是部署纯 yaw 场景下的左右协调、上身稳定和动力学匹配改善，而不是单独 yaw 奖励项变强。

## 4.28
1.pretrain sym10_then_sym100 比 sym100 效果好很多，前者的策略的鲁棒、原地转都远超后者，前者的原地转的时候脚都没有抬起来，像灌了铅一样：从零开始时 policy 还没有学会走，强行给 100，最容易降低 symmetry loss 的方式是输出保守、幅度小、左右严格一致的动作，而不是先学会有相位、有支撑、有摆动的 gait。

## 4.29
1.20ms pro half walk 成功训练出来了，今天训的是10ms的pro half walk。
2.学习 SONIC 。

## 5.6
1.10ms 的 pro half walk  训练完毕，吹毛求疵的话就是 yaw only 的命令仍然支持不太好。
首先是用 yaw only 命令 resume 小 iter + yaw only 时的x/y速度漂移奖励惩罚看效果，sim2sim的 yaw only 命令遵循很好，基本是绕着 base 中轴线转，问题就是模型遗忘了正常的走路姿态该怎么走（迈步一只脚大一只脚小），分析原因：
1. yaw-only 原地转向天然允许左右脚不对称：转左时一只脚迈大、一只脚迈小是低成本旋转策略。mirror_loss 只能约束“左转策略镜像后等于右转策略”，不能约束“同一个 yaw 命令下左右步幅相等”。
2. AMP
3. PPO 是 on-policy。

2.横移左右脚一前一后；静止的时候站立对齐；横移的时候feet太近。

## 5.7
yaw mix 的comand 采样占比 0.5而不是1.0 在 原10ms 的 pro half walk 的权重上resume。

## 5.8
1.**yaw的轴心偏移问题核心：gate reward、yaw command 组合**。
**单单 yaw only command没有nomal command，一步大一步小**，把 symmetry coeff 调大没有作用；所以**只能用 mix command**（yaw only 占比自己调）。

## 5.11
1.还是yaw的轴心偏移问题：
**cycle_path_symmetry**管“一个周期内左右脚运动量是不是对称”。
如果两个脚都走得很少，cycle_path_symmetry 也可能很小；所以还需要 **yaw_only_feet_midpoint_xy_l2** 和 **yaw_only_lin_vel_x/y_l2** 一起约束。
2.死区的问题好像越来越小了，莫非是**min abs激活值**的问题？
## 5.12
1.**feet_slide.weight**: -0.25 -> -0.35 + 删除 yaw_only_feet_stance_width :原地yaw时脚直接拖地，抬不起来了。
解决方案：加入feet_air_time_positive_biped，并且加入 yaw-only command mask ，小幅提高yaw_only_gait_feet_frc_perio，恢复feet_slide.weight: -0.25：没给commands，机器人**原地轻微晃动，原地yaw启动时些许拖地的倾向**。

## 5.13
1.小幅度提高stand_still_vel/stand_still_feet_motion_penalty以及yaw_only_feet_air_time，把 yaw ratio 从0.5降到0.4,在训练一版。

## 5.14
1.yaw和normal都解决了，y command出问题了，如何复现？

2.move_operate方面，policy完全没学到height命令的跟踪，track_height_exp≈2.5-2.7 基本等价于 0.33-0.36m 误差。怀疑是**rel_standing_envs**=0.4,这个设计的有问题，会把height command清零，因为如果是这样的话，那么stand的时候height为0,那么0.6-0.95的正常command不就都是高度到顶了也就是正常站立了？

## 5.15
1.y command 还是有问题，需要动奖励函数。

2.move_operate 还在训练。

## 5.19
1.move_operate的 **height 不跟踪**的问题，把rel_standing_envs修了但是还是没改进，所以很可能是
```python
reward = exp(-abs(base_height - target_height) / 0.3)
weighted = 8.0 * reward
```
的问题 ：**std=0.3** 对 0.35m 的 height command range 太松。最低目标 0.6 时误差 0.35~0.36m 仍然有 2.4~2.5 / 8 的加权奖励，这会让策略忽略 height。如果 target_h 在 [0.6, 0.95] 均匀采样，而机器人永远保持 base_h=0.95，weighted 期望 ≈ 4.72 / 8，和 TensorBoard 里 track_height_exp ≈ 4.6~4.7 基本一致。

## 5.20
1.**pro half walk**：加大 `yaw_only_feet_midpoint_xy_l2` 和 `yaw_only_feet_cycle_path_symmetry_l1` 后，yaw-only 的平面轴心/轨迹指标确实改善了，但 policy 找到了副作用解：转左时左脚作为内侧脚低抬、贴地扫，转右时右脚低抬。原因是这两个 reward 都只看脚的 `x/y` 平面，不约束 `z` 高度；`yaw_only_feet_air_time` 只约束腾空时间，不约束腾空高度。

应对方法是补上 yaw-only 下的垂直 clearance 约束，而不是继续加大 `yaw_only_gait_*` 或继续加大平面轴心项。已新增：

- `yaw_only_swing_clearance_l1`

- 只在 yaw-only command 下生效

- 用 gait swing phase 判断当前该抬哪只脚

- 用 `swing_foot_z - opposite_ankle_z` 作为 clearance

- `target_clearance=0.04m`

- `weight=-2.0`

- 保持 `yaw_only_gait_*` 不变，避免重新引入 gait/contact 过强导致拖脚

2.**总结 move_operate 的问题和方法**：
`pro_half_walk_move_operate` 的 height command 链路没有断，policy 能看到 `cmd_h`，reward 也在算，但训练学到的是“基本忽略 height、保持接近默认高度”。`std=0.3` 时 height reward 太宽，默认站高就能骗到约 `4.57/8`；改到 `std=0.07` 后更真实，`track_height_exp` 降到约 `1.5~1.9/8`，说明之前高分主要是假象，但仍未真正学会 command-conditioned height tracking。

roll/pitch 能学而 height 不能学，原因是二者难度不同。roll/pitch 不响应 command 的 baseline 分低、姿态反馈直接、动作代价较小；height 需要持续改变髋膝踝构型，会被 AMP、gait、能耗、action rate、关节默认位和接触稳定性共同拉回正常站高。当前 `height` 是高代价、低收益 command，所以 policy 选择忽略它。

应对方法：

- 已修正 `rel_standing_envs` 语义：standing 只清 `x/y/yaw`，保留 `roll/pitch/height`。
- 已用 `stand_still_exp_rph_neutral` 替代旧 `stand_still_exp`，避免零速度但非 neutral RPH 时强拉默认腿姿态。
- 已将 `track_height_exp` 收紧到 `std=0.07`，作为近目标精修项。
- 已新增 `base_height_error_l1`，作为远目标拉力，当前保守权重 `-2.0`。

## 5.21
1. move_operate，接着昨天的做：
   新增 joint_deviation_l1_height_neutral，当 cmd_h < 0.90 时关闭腿部默认位惩罚；
	 把 joint_deviation_legs 切到 height-neutral gated 版本；
	 显式覆盖 amp_reward_coef = 0.1。
	 **成功蹲下**

## 5.22-23
1.**command 死区的问题**：mlp相对线性，当 rel_stand_env = 0.4这个比例的时候，mlp policy 偏向于站立，这种策略在训练的时候能拿到很多奖励，所以有较大死区；把 rel_stand_env 调到 0.2 死区变小。如果是 MOE 就不会有这个问题。
2.加入 **gait free** 训练范式。
## 5.25
gait free 训练范式，如果不改默认 reward 权重的话，效果很差（**小碎步搓脚**），分析 tensorboard，改动如下权重：
- contact_no_slip = -2.0          # -0.5 -> -2.0
- feet_air_time_positive_biped = 12.0  # 4.0 -> 12.0
- stand_still_double_support = 3.0     # 0.5 -> 3.0
## 5.26
1.昨天改的权重效果一样差，还是**小碎步搓脚**，分析原因：
- contact_no_slip 和 feet_air_time/contact_alternation/gait_cycle 有冲突。contact_no_slip 在接触时惩罚脚水平速度，权重从 -0.5 到 -2.0 后，策略最简单的降惩罚方式是减少步幅、减少支撑脚相对运动，甚至不走，而不是“抬高脚再迈出去”。这解释了小碎步、搓脚减少但抬脚也没起来。现在的问题是机器人还没学会“抬脚、清晰单支撑、再落脚”。在这种阶段，contact_no_slip 会惩罚所有接触脚的水平速度。只要脚有擦地、半接触、落脚瞬间带水平速度、toe-off/roll-over，这个 penalty 都会很密集地打下来。

- feet_air_time_positive_biped 本身是**稀疏项**。它只有在 single stance 时才给值，策略如果已经倾向双支撑/小碎步，这个项即使权重从 4 到 12，也很难提供足够早期梯度。结果是加权贡献略涨，但裸值反而下降。

2.把昨天的权重减少一些；新增一个 **swing clearance** ，没有用现有的 feet_clearance 因为他是依靠 pelvis 和 foot的相对高度来算的，对于 terrain 和 RPH command 会失效。

——训练效果很不稳定，旧训练范式给 policy 一个明确时钟：什么时候该摆腿、什么时候该支撑、哪个脚该动。新范式是 gait-free，但奖励基本都依赖接触事件已经出现。早期随机策略还没形成稳定单支撑时，feet_air_time、contact_alternation、gait_cycle 都很**稀疏**，policy 很难从零学出稳定节律。

## 5.27
1.move_operate 的 zero command 问题：不给 command 也会踏步后退，而且频率和前进 command 很像。

本质不是简单的 stand still reward 权重不够，而是有 RPH 后任务已经不是 moving / stand still 二分类。现在至少要分：
- `vel_active + pose_neutral`：普通走路。
- `vel_active + pose_active`：带 RPH 的走路。
- `vel_zero + pose_neutral`：默认站立，正常高度是 `height=0.95`。
- `vel_zero + pose_active`：原地做 roll/pitch/height，不应该被拉回默认腿姿态。

之前 `stand_still_exp_rph_neutral` 只在 RPH 中性时奖励默认站立，但连续采样下 RPH 正好中性的概率接近 0；而 `vel_zero + pose_active` 又没有像样的“原地完成姿态”的约束，所以 policy 会把学到的 gait attractor 带到 zero command 里，表现成原地踏步甚至后退。

改法：
- 新增显式 command mode：`neutral_stand_ratio=0.15`，固定 `x/y/yaw=0, roll=0, pitch=0, height=0.95`。
- 新增 `in_place_pose_ratio=0.25`，固定 `x/y/yaw=0`，但随机 RPH，用来学原地姿态控制。
- 剩余 `0.60` 走 locomotion，RPH 仍按 curriculum 打开。
- 新增 zero-velocity 约束：`zero_velocity_base_xy_vel_l2 / zero_velocity_yaw_vel_l2 / zero_velocity_feet_xy_vel_l1`，只看 `x/y/yaw≈0`，不要求 RPH neutral，用来压原地 RPH 下的后退、自转、踏步搓脚。
- RPH curriculum 每 stage 最小步数暂定 `3000`，避免太快放开完整 RPH，但也不把训练拖得过严。

## 5.28
1.move_operate 新一版可视化后，问题要拆开看：
- neutral stand 基本解决，不是所有 zero command 都不稳。
- 向前走是低频高抬腿、重砸脚。
- 向后退是高频搓地。
- 侧向走时一只脚会踩到另一只脚。
- `x/y/yaw=0` 单独给 RPH 时，机器人会通过挪脚调整支撑来完成姿态，而不是脚固定在原地做姿态控制。

所以根因不是单纯 reward 权重不够，而是当前 gait-free move operate 里两个结构缺口：
- locomotion 缺方向相关的足端几何和落地质量约束。
- in-place RPH 缺足端 world anchor，`zero_velocity_feet_xy_vel_l1` 只能压瞬时速度，压不住慢慢挪脚。

这次一次性改动：
- 新增 `in_place_feet_anchor_xy_l1`：进入 in-place pose command 周期时记录两脚 world xy，周期内惩罚脚相对 anchor 漂移。
- 启用 `feet_sagittal_align_lateral` 和 `feet_lateral_distance_lateral`：侧移时约束左右脚前后对齐和横向站宽，避免横移踩脚。
- 新增 `touchdown_foot_vel_z_l2`：惩罚 touchdown 瞬间脚向下速度过大，用来处理前进重砸脚。
- `contact_no_slip` 从 `-1.0` 调到 `-1.5`；`gait_cycle_reward_event` 从 `4.0` 调到 `6.0`，并把 early/late touchdown penalty 加强，用来压后退高频搓地和错误接触周期。
